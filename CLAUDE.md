# the-purpose

## 目的

「今日の目的・制約・やること」と「時間ベースの目標」を1画面で管理・表示するツール。
日ごとに3つのテキストと時間付き目標を事前登録し、当日リアルタイムで表示する。

## 設計・環境

| 項目 | 内容 |
|------|------|
| ローカル URL | `git13.local` |
| 本番 URL | `on24.motisan.info` |
| PHP | 8.2 |
| データ | `data/data.json`（JSON管理、gitignore対象） |
| 画面サイズ | 1920x1080 固定（モバイル非対応） |

### ページ構成

| ページ | ファイル | 役割 |
|--------|---------|------|
| 表示ページ | `index.php` | 今日の3テキスト＋現在目標を大きく表示 |
| 管理ページ | `admin.php` | 日付ごとの登録・編集・過去一覧 |

### 目標進行ロジック

- 目標は時刻順に並ぶ（時刻 = デッドライン）
- 表示：未達成の中で最初の目標を1つだけ表示
- 締切前：チェックボックスで手動完了 → 次の目標へ
- 締切超過：自動で達成済み扱い・チェック不可でスキップ
- JS の setInterval（1分ごと）で時刻チェック
- 全目標完了または締切超過 → 「今日の目標完了」表示

### データ構造（data/data.json）

```json
{
  "2026-05-01": {
    "items": ["テキスト1", "テキスト2", "テキスト3"],
    "goals": [
      { "time": "12:00", "text": "買い物", "achieved": false },
      { "time": "16:00", "text": "お風呂掃除", "achieved": false }
    ]
  }
}
```

### ファイル構成

```
/13/
├── index.php
├── admin.php
├── .htaccess
├── .gitignore          # data/ を含む
├── css/
│   ├── display.css
│   └── admin.css
├── js/
│   ├── display.js      # 時刻監視・チェックボックス処理
│   └── admin.js        # フォーム操作・過去一覧
├── api/
│   ├── get_day.php     # GET ?date=YYYY-MM-DD
│   ├── save_day.php    # POST: items + goals保存
│   ├── toggle_goal.php # POST: 達成状態のトグル
│   └── get_all.php     # GET: 全日付データ
└── data/
    └── data.json
```

### コーディング規約（php-env-a 準拠）

- 絶対パス不使用。`dirname(__FILE__)` 起点の相対パスで統一
- ファイルは機能単位でモジュール分割
- `.htaccess` に `DirectoryIndex index.php` を記述
- UI：ライトテーマ・バニラCSS・1920x1080固定

---

## ドキュメント管理ルール

### ファイルの役割分担

| ファイル | 役割 | 読むタイミング |
|---|---|---|
| `CLAUDE.md` | プロジェクトルール＋アクティブな todo/issue のみ | 毎チャット必読 |
| `.claude-codex/CURRENT.md` | 直近の完了内容・次にやること・注意事項 | 毎チャット必読 |
| `.claude-codex/change/*.md` | 完了した作業の詳細ログ | 必要なときだけ |

### todo のルール
- `[ ]` のみここに記載する
- `[x]` にしたら**その行を削除**する（記録は change/ の変更ログが担う）

### issue のルール
- バグ・不具合・改善点が見つかったら、**まずここに `[ ]` で登録してから対処**する
- 対処完了後は `[x]` にしてから**その行を削除**する

### 変更ログのルール
- 作業の変更内容は `.claude-codex/change/作業内容.md` に記録する
- 記録フォーマット：タイムスタンプ・変更内容・意図を羅列する

### セッション引き継ぎのルール
- チャット終了時に `.claude-codex/CURRENT.md` を最新状態に更新する
- 新チャット開始時は **CLAUDE.md → CURRENT.md** の順に読めば文脈が揃う状態を保つ

---

## issue
（現在アクティブな issue なし）

## todo
（現在アクティブな todo なし）
